const myModule = require('./module.js');

// If the module is empty, it will return an empty object "{}".
// If the module has exports, it will return the exports.
// In this case an object with name, age, and luckyNum properties.
console.log(myModule);